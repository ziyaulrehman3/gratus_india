import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "./components/Header";
import Footer from "./components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Gratus India | Premium Steel Manufacturing & Export",
    template: "%s | Gratus India",
  },
  description:
    "Gratus India is a leading manufacturer and exporter of steel products, steel wire items, pipes, tubes, sheets, coils, and industrial steel solutions.",
  keywords: [
    "steel manufacturer",
    "steel exporter",
    "steel wire products",
    "steel pipes",
    "steel tubes",
    "industrial steel",
    "India steel company",
    "Gratus India",
  ],
  icons: {
    icon: "/logo.png",
    shortcut: "/logo.png",
    apple: "/logo.png",
  },
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
